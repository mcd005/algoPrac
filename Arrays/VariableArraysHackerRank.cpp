#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

//https://www.hackerrank.com/challenges/variable-sized-arrays

int main() {
    vector <vector<int>> arrays;

    int numArrays, queries;

    scanf("%d %d", &numArrays, &queries);

    for (int i = 0; i < numArrays; i++){
        int sizeArray;
        cin >> sizeArray;

        for (int j = 0; j < sizeArray; j++){
            arrays[i]
        }
    }
    
    return 0;
}

